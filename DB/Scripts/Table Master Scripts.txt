  CREATE TABLE `BCS`.`ROLE` (
  `ROLE_ID` BIGINT NOT NULL,
  `ROLE_DESC` VARCHAR(45) NOT NULL,
  `CREATE_TS` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `LST_UPDT_TS` TIMESTAMP NOT NULL DEFAULT now() on update now(),
  PRIMARY KEY (`ROLE_ID`),
  UNIQUE INDEX `ROLE_ID_UNIQUE` (`ROLE_ID` ASC));

  CREATE TABLE `BCS`.`PRIV` (
  `PRIV_ID` BIGINT NOT NULL,
  `PRIV_DESC` VARCHAR(45) NOT NULL,
  `CREATE_TS` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `LST_UPDT_TS` TIMESTAMP NOT NULL DEFAULT now() on update now(),
  PRIMARY KEY (`PRIV_ID`),
  UNIQUE INDEX `PRIV_ID_UNIQUE` (`PRIV_ID` ASC));

  CREATE TABLE `BCS`.`ROLE_PRIV` (
  `ROLE_ID` BIGINT NOT NULL,
  `PRIV_ID` BIGINT NOT NULL,
  `CREATE_TS` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `LST_UPDT_TS` TIMESTAMP NOT NULL DEFAULT now() on update now(),
  PRIMARY KEY (`PRIV_ID`, `ROLE_ID`),
  INDEX `FK_ROLE_ID_idx` (`ROLE_ID` ASC),
  CONSTRAINT `FK_ROLE_ID`
    FOREIGN KEY (`ROLE_ID`)
    REFERENCES `BCS`.`ROLE` (`ROLE_ID`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `FK_PRIV_ID`
    FOREIGN KEY (`PRIV_ID`)
    REFERENCES `BCS`.`PRIV` (`PRIV_ID`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION);
	
	ALTER TABLE `BCS`.`ROLE_PRIV` 
	ADD UNIQUE INDEX `PRIV_ID_UNIQUE` (`PRIV_ID` ASC),
	ADD UNIQUE INDEX `ROLE_ID_UNIQUE` (`ROLE_ID` ASC);

CREATE TABLE `BCS`.`BANK_USER` (
  `USER_ID` BIGINT NOT NULL AUTO_INCREMENT,
  `USER_NAME` VARCHAR(60) NOT NULL,
  `PASSWORD` VARCHAR(70) NOT NULL,
  `STATUS` VARCHAR(10) NOT NULL,
  `ROLE_ID` BIGINT NOT NULL,
  `CREATE_TS` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `LST_UPDT_TS` TIMESTAMP NOT NULL DEFAULT now() on update now(),
  PRIMARY KEY (`USER_ID`),
  UNIQUE INDEX `USER_NAME_UNIQUE` (`USER_NAME` ASC),
  INDEX `FK_BK_USR_ROLE_idx` (`ROLE_ID` ASC),
  CONSTRAINT `FK_BK_USR_ROLE`
    FOREIGN KEY (`ROLE_ID`)
    REFERENCES `BCS`.`ROLE` (`ROLE_ID`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION);